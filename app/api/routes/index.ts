/**
 * Sangwari AI — Unified API Routes Index
 *
 * Import all routes from here for a single entry point:
 *   import { authRoutes, schemeRoutes, complaintRoutes, voiceRoutes, userRoutes } from '@/api/routes';
 */
export { authRoutes } from './auth';
export { schemeRoutes } from './schemes';
export { complaintRoutes } from './complaints';
export { voiceRoutes } from './voice';
export { userRoutes } from './user';
export * from './notifications';

